/**
 * Colour tokens of the storybook's own, so the KPI stories can name them without a cast — the
 * same two lines a consuming app writes. The colours live in stories.css.
 */
declare module "@andrewmclachlan/moo-ds" {
    interface HueRegistry {
        income: true;
        expense: true;
    }
}

export { };
